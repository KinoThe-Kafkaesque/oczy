# Plastic Cortex

Correction-gated recurrent / state-space backbone with fast-weight (TTT-style) session adaptation.

## Quick start

```bash
uv sync
uv run pytest
```

## Project position in the stack

```text
activation / context

fast state

fast weights

neural memory

slow weights / priors
```

See `../experiments.txt` for the full architecture thesis.
