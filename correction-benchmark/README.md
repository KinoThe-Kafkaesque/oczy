# Correction-to-Competence Benchmark

Eval tasks and metrics for correction uptake, transfer, scope control, forgetting, compression, and identity drift.

## Quick start

```bash
uv sync
uv run pytest
```

## Project position in the stack

```text
activation / context

fast state

fast weights

neural memory

slow weights / priors
```

See `../experiments.txt` for the full architecture thesis.
